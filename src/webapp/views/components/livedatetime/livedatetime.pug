mixin liveDateTime(date, locale)
  .liveDateTime(data-component="liveDateTime" data-date=date data-locale=locale)&attributes(attributes)
    p.liveDateTime__date= date.toLocaleDateString(locale)
    p.liveDateTime__time= date.toLocaleTimeString(locale)
