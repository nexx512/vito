extend ../layout/index.pug

include /notification/notification
include /form/form
include /weekcycletimes/weekcycletimes
include /section/section
include /button/button

block breadcrumb
  h2= t("warmwater.circulation")

block main
  .warmwaterCirculation__topSpacer
  +sections().warmwaterCirculation__sections
    +section("circulationTimes", t("warmwater.circulation.times"), true)
      +form("PUT", "/warmwater/circulation/times").warmwaterCirculation__timesForm
        +weekCycleTimes(model)
        +buttonPrimary(t("savechanges"))(type="submit")
    +section("tab2", "Tab 2")
      | blablabla
